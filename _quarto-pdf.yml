# Quarto profile: `quarto render --profile pdf --to pdf`
# Merged on top of _quarto.yml. Default `quarto render` stays HTML-only.

project:
  output-dir: _book-pdf
  resources:
    - cover.png

format:
  pdf:
    documentclass: scrbook
    classoption:
      - oneside
      - open=any
      - numbers=noenddot
      - fontsize=11pt
    pdf-engine: lualatex
    # The detailed Contents page is contents.qmd (also included in
    # the preface and the introduction). Do not also emit a generated TOC.
    toc: false
    toc-depth: 3
    lot: false
    lof: false
    number-sections: true
    papersize: a4
    geometry:
      - top=24mm
      - bottom=24mm
      - left=26mm
      - right=26mm
    linestretch: 1.18
    mainfont: "TeX Gyre Pagella"
    sansfont: "TeX Gyre Heros"
    monofont: "Latin Modern Mono"
    colorlinks: true
    fig-align: center
    fig-pos: "H"
    keep-tex: false
    cite-method: citeproc
    link-citations: true
    filters:
      - filters/svg-to-pdf.lua
    include-in-header:
      - tex/book-pdf-header.tex
      - tex/book-pdf-titlepage.tex
    header-includes:
      - \usepackage{float}
